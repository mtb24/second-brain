Run this command on the VPS and return exactly one line: `done` or `failed: <reason>`.

Command:
bash ~/brain/tournament/scripts/strategy-master-cron.sh

Do not run the strategy workflow directly in this cron turn; delegate to the script above so model fallback policy is enforced.
