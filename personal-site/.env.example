# Copy to ../.env (repo root) or .env here — never commit real secrets.
# See BRAIN.md for the full ~/brain/.env shape.

# Backblaze (optional — legacy tooling / one-off bucket ops; adventure gallery is on-VPS now)
# B2_BUCKET_NAME=kendowney-assets
# B2_PUBLIC_URL=https://f004.backblazeb2.com/file/kendowney-assets

# Optional: override adventure image base in dev/build (default `/images/adventures`)
# VITE_ADVENTURE_IMAGE_BASE=/images/adventures
